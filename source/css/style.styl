:root {
  --shadow_grey: rgba(153, 153, 153, 1);
  --box_grey: rgba(237, 237, 237, 1);
  --text_orange: rgba(252, 100, 35, 1);
}

html, body {
  height: 100%;
  margin: 0px;
  padding: 0px;
}

.mid_container {
  height: auto;
}

@import "search.styl"
/* 本地搜索器的样式 */

@import "block.css"
@import "tool.css"
@import "font.css"