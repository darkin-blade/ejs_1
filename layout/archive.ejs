<div>
  <% page.posts.forEach(function (post) { %>
    <p><%= date(post.date, "YYYY-MM-DD") %></p>
    <a href="<%- url_for(post.path) %>"><%= post.title %></a>
  <% }) %>
<div>

<%- partial('partial/paginator.ejs') %>